# Leave unset for local dev - Vite's dev-server proxy (vite.config.js) forwards
# /api requests to the backend. For a production build, point this at your
# deployed API, e.g. https://your-api.onrender.com/api/v1
VITE_API_URL=
